# EmlakMaster — opsiyonel ortam değişkenleri
# Bu dosyayı .env olarak kopyalayıp değerleri doldurun.
# Uygulama Firebase firebase_options.dart ile çalışır; .env zorunlu değildir.

# Örnek (ileride API anahtarı eklerseniz):
# API_KEY=
# FIREBASE_USE_EMULATOR=false
