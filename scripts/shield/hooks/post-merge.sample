#!/bin/sh
# Git post-merge: pull sonrası shield çalıştır (kendini onar).
# Kurulum: scripts/shield/install_git_hooks.sh
set -e
cd "$(git rev-parse --show-toplevel)"
SCRIPT="scripts/shield/shield.sh"
[ -x "$SCRIPT" ] && "$SCRIPT" --quiet || true
exit 0
